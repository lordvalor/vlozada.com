on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
    
name: 🚀 11ty Build and FTP Deploy
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4

    - name: Use Node.js 20
      uses: actions/setup-node@v2
      with:
        node-version: '20'
      
    - name: 🔨 Build Project
      run: |
        npm install
        npm run build
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ftp.vlozada.com
        username: gitdeployment@vlozada.com
        port: 21
        protocol: ftp
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ${{ github.workspace }}/dist/
        exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**